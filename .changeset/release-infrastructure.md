---
---

Release infrastructure only — no user-facing changes. Pre-release mode (`beta`) entered; package versions bootstrapped to the `0.1.0-beta.x` line per issue #8. First published version will be `0.1.0-beta.1` on the `beta` dist-tag with npm provenance once the next real changeset lands.
