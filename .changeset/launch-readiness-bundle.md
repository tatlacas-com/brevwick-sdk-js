---
---

Docs-only update across `packages/sdk/README.md` and `packages/react/README.md` plus four new example apps (vite-react, CRA, Remix, Astro) and a no-build vanilla static example. No public-API or runtime changes; no version bump warranted. The empty front-matter satisfies the changeset gate without producing a release entry.
