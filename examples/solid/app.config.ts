import { defineConfig } from '@solidjs/start/config';

export default defineConfig({
  // Empty preset → SolidStart defaults to Node SSR. Keep the example small;
  // a deployer who wants a different target (Vercel, Cloudflare) flips this
  // line and ships.
});
