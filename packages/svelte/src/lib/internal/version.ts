declare const __BREVWICK_SVELTE_VERSION__: string;

/**
 * Semantic version of the installed `@tatlacas/brevwick-svelte` package.
 * Surfaced at runtime so consumers can include it in bug issues or
 * diagnostic payloads.
 */
export const BREVWICK_SVELTE_VERSION: string = __BREVWICK_SVELTE_VERSION__;
